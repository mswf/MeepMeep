
WorldData = class(WorldData, function(self, serializedData)
	self.broadcaster = Broadcaster()

	-- todo: handle loading
end)

function WorldData:serialize()
	local serializedData = {}

	return serializedData
end
